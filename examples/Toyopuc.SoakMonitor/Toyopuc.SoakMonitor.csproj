<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\..\src\Toyopuc\Toyopuc.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <RuntimeIdentifiers>win-x64</RuntimeIdentifiers>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>Toyopuc.SoakMonitor</AssemblyName>
    <Title>Toyopuc SoakMonitor</Title>
    <Product>Toyopuc SoakMonitor</Product>
    <Description>Dedicated long-duration monitoring CLI for the Toyopuc .NET library.</Description>
  </PropertyGroup>

  <Target Name="DeletePublishSidecars" AfterTargets="Publish">
    <ItemGroup>
      <PublishSidecars Include="$(PublishDir)*.pdb" />
      <PublishSidecars Include="$(PublishDir)*.xml" />
    </ItemGroup>
    <Delete Files="@(PublishSidecars)" />
  </Target>

</Project>
